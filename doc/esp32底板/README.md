# ESP32S3-BASE01A V1.0 扩展底板

`esp32底板.jpg` 是当前使用的扩展底板参数和插针图。图中可确认：

- 型号为 `ESP32S3-BASE01A V1.0`；
- 每个 GPIO 提供黄色 `S`、红色 `5V`、黑色 `G`、蓝色 `3.3V` 四排插针；
- 底板适配常见 44 引脚 ESP32-S3 开发板。

该图片不是电气原理图，不能据此推断保护、电源纹波、各路最大安全电流或信号电平转换能力。项目模块接线以 `../模块接线最终规划.md` 为准。

图片 SHA-256：`9CCFDB6185807BEAB8967C85B87EDF7F626D1B561E695B8CAEDC81A23312C826`。
