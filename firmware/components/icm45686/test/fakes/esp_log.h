#pragma once

#define ESP_LOGI(tag, format, ...)                                             \
  do {                                                                         \
    (void)(tag);                                                               \
  } while (0)
